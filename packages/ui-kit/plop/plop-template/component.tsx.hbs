import type { ComponentPropsWithRef } from "react";
import { forwardRef } from "react";

export interface {{pascalCase name}}Props extends ComponentPropsWithRef<"div"> {
  // 여기에 props 타입을 정의하세요
}

const {{pascalCase name}} = forwardRef<HTMLDivElement, {{pascalCase name}}Props>(function {{pascalCase name}}(
  props,
  ref
) {
  const { ...rest } = props;

  return (
    <div ref={ref} {...rest}>
      <h1>{{pascalCase name}} 컴포넌트</h1>
    </div>
  );
});

{{pascalCase name}}.displayName = "{{camelCase name}}";

export default {{pascalCase name}};
